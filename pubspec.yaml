name: country_phone_validator
description: A Dart package for validating phone numbers based on country codes. Includes country info retrieval.
version: 1.0.1
repository: https://github.com/ceto2016/country_phone_validator
# repository: https://github.com/my_org/my_repo
environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=2.0.0"
# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

example:
  main: example/example.dart